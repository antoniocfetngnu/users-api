services:
  users-api:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/root/data  # Persist SQLite database
    environment:
      - GIN_MODE=release
    restart: unless-stopped